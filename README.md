# recipe-app-api
Recipe API project


## STEPS
1. List all dependencies and their versions needed in the project (Django and djangorestframework) in requirements.txt
2. Create Dockerfile, define steps needed to build the image, (alpine is a lightwight linux version of python without unnecessary dependencies)